node_modules
dist
.env.local

# Bytecode de los scripts de Python: se regenera solo en cada corrida.
__pycache__/

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/
.gstack/

# Listado del equipo: mails personales, y este repo es publico.
*.local.sql

# Permisos y ajustes de Claude Code propios de cada maquina.
# Lo compartido va en .claude/settings.json; lo personal, aca.
.claude/settings.local.json
